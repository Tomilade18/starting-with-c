#include <stdio.h>
#include <string.h>

int main()
{
    int l1,l2,i;
    char s1[30]="jenny";
    char s2[]="khatri";
    // strcat(s1,s2);

    l1 = strlen(s1);
    l2 = strlen(s2);
    
    printf("%s\n", s1);
    puts(s2);
}